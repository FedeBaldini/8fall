import type { NextPage } from "next";
import { Layout } from "../components/Layout/Layout";
import { Logo } from "../components/Logo";

const Home: NextPage = () => {
  return <Layout>Uall</Layout>;
};

export default Home;
